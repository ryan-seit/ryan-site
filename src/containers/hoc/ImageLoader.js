import React from "react";
import Spinner from "../UI/Icons/Spinner";

const ImageLoader = props => {
	console.log(props);
	// const imgObj = new Image();
	// imgObj.onload = <Spinner />;
	// imgObj.src = props.imgSrc;
	// imgObj.alt = props.imgAlt;

	// console.log(imgObj);
	// return imgObj;
};

export default ImageLoader;
